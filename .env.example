# Env vars — copy this file to .env.local and fill in values

# After deploying social-api/worker.js to Cloudflare, paste the Worker URL here:
VITE_SOCIAL_API_URL=https://tulip-social-api.YOUR-SUBDOMAIN.workers.dev
